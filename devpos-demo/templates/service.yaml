apiVersion: v1
kind: Service
metadata:
  name: {{ include "devpos-demo.fullname" . }}
  labels:
    {{- include "devpos-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devpos-demo.selectorLabels" . | nindent 4 }}
