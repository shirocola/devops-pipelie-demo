replicaCount: 2

image:
  repository: gcr.io/your-gcp-project-id/devops-demo
  pullPolicy: IfNotPresent
  tag: "dev-latest"

service:
  type: ClusterIP
  port: 80

resources:
  limits:
    cpu: 200m
    memory: 256Mi
  requests:
    cpu: 100m
    memory: 128Mi
